import React from 'react';
import {ScrollView} from 'react-native-gesture-handler';

import Items from '../../../components/SimilarProducts/Items';

export default function SimilarProducts() {
  return (
    <ScrollView>
      <Items />
    </ScrollView>
  );
}
